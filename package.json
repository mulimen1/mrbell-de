{
  "name": "mrbell-de",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mr. Bell — WhatsApp AI-Assistent für deutsche SMB",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^17.5.0",
    "@anthropic-ai/sdk": "^0.32.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
